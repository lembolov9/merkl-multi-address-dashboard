export default function HomePage() {
    return (
        <main style={{ padding: '2rem' }}>
            <h1>Merkl Multi-Address Dashboard</h1>
            <p>Navigate to <a href="/dashboard">/dashboard</a> to get started.</p>
        </main>
    );
}
